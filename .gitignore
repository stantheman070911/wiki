# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Thumbnails / temp
._*
*~
.Spotlight-V100
.Trashes

# Local app/session state
.claude/*
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# …but the vault's own skills are tooling, not state — they are versioned
!.claude/skills/

# Obsidian community plugin binaries (installed locally)
.obsidian/plugins/

# Local JavaScript dependencies
node_modules/
