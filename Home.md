---
tags: [home]
---

# THE WIKI

A centralized knowledge vault for business strategy, social media strategy, tactics, and frameworks — every useful lesson turned into lasting, searchable institutional knowledge.

## Sections

- [[01-Business-Strategy/README|01 — Business Strategy]] — business models, growth, ops, decision-making *(`$= dv.pages('"01-Business-Strategy"').where(p => p.file.name != "README").length` entries)*
- [[02-Social-Media-Strategy/README|02 — Social Media Strategy]] — platform strategy, content strategy, growth tactics *(`$= dv.pages('"02-Social-Media-Strategy"').where(p => p.file.name != "README").length` entries)*
- [[03-Tactics-and-Playbooks/README|03 — Tactics and Playbooks]] — step-by-step, repeatable how-tos *(`$= dv.pages('"03-Tactics-and-Playbooks"').where(p => p.file.name != "README").length` entries)*
- [[04-Frameworks-and-Mental-Models/README|04 — Frameworks and Mental Models]] — reusable thinking tools and mental models *(`$= dv.pages('"04-Frameworks-and-Mental-Models"').where(p => p.file.name != "README").length` entries)*
- [[05-Intelligence-and-Research/README|05 — Intelligence and Research]] — market/competitor intel, notable examples, research findings *(`$= dv.pages('"05-Intelligence-and-Research"').where(p => p.file.name != "README").length` entries)*
- [[06-Source-Library/README|06 — Source Library]] — raw source materials and extraction notes, organized by source *(`$= dv.pages('"06-Source-Library"').where(p => p.file.name != "README").length` items)*
- [[07-Articles/README|07 — Articles]] — composed, outward-facing pieces synthesized from multiple entries *(`$= dv.pages('"07-Articles"').where(p => p.file.name != "README").length` articles)*
- [[_Inbox/README|Inbox]] — unprocessed material awaiting triage

## Navigate

- **New entry** → duplicate `00-Templates/Entry-Template.md` (English) or `Entry-Template-ZH.md` (中文) into the right sub-topic folder
- **Unsure where it goes** → drop it in `_Inbox/` first; see placement rules in [[README]]
- **Find a topic** → Quick Switcher (`Cmd+O`), global search (`Cmd+Shift+F`), or the tag pane
- **Browse tags** → [[Tags]] — the controlled, faceted vocabulary (`topic/` `person/` `source/`)
- **Browse by topic across languages** → [[Topic-Index]] — English + Chinese entries per topic
- **See how everything connects** → open Graph view from the left ribbon

> Domain indexes are generated by the **Dataview** plugin — install it (Community plugins → Browse → *Dataview*) so the index tables render.

## Principles

- Condense, don't summarize — every entry should be usable without reading the source.
- Prefer frameworks and mental models over one-off facts — they generalize.
- Cite the source and date on every entry, and link it, for traceability.
- Update existing entries when better information arrives, rather than creating duplicates.

---
*Full conventions and structure: [[README]]*
